# KubeVirt VM — attaches the 'br0-network' NAD (OVS bridge on dedicated NIC)
apiVersion: kubevirt.io/v1
kind: VirtualMachine
metadata:
  name: vm-br0
  namespace: vmtest
spec:
  runStrategy: Always
  template:
    metadata:
      labels:
        kubevirt.io/domain: vm-br0
    spec:
      domain:
        devices:
          disks:
          - disk:
              bus: virtio
            name: rootdisk
          interfaces:
          - name: br0-net
            bridge: {}
        resources:
          requests:
            memory: 2Gi
      networks:
      - name: br0-net
        multus:
          networkName: vmtest/br0-network
      volumes:
      - name: rootdisk
        containerDisk:
          image: quay.io/kubevirt/cirros-container-disk-demo:latest
