apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: vlan100-net
  namespace: default
spec:
  config: |
    {
      "cniVersion": "0.3.1",
      "name": "vlan100-net",
      "type": "bridge",
      "bridge": "br-vlan100",
      "ipam": {
        "type": "static"
      }
    }
