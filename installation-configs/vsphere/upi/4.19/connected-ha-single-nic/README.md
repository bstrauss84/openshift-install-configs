# Connected HA single NIC

Minimal connected example. OVN-Kubernetes is the default CNI. For bare metal with platform.baremetal and VIPs, installer handles keepalived/haproxy. For platform: none you must provide external LBs.
