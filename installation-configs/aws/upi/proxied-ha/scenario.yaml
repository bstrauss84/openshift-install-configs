name: aws-upi-proxied-ha
platform: aws
method: upi
networking:
  type: OVNKubernetes
  machineNetworkCIDR: 10.90.0.0/24
loadBalancer: external
addressing: dhcp
nodes:
  controlPlane: 3
  workers: 3
proxy:
  enabled: true
notes:
  - You must provide external LB + DNS (api, api-int, *.apps)
artifacts:
  - install-config.yaml
